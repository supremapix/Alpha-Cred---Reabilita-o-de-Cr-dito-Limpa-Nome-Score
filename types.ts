
export interface ServiceCardProps {
  icon: string;
  title: string;
  description: string;
  items?: string[];
  ctaText: string;
  color?: string;
}

export interface TestimonialProps {
  name: string;
  age: string;
  text: string;
  rating: number;
  role?: string;
}
